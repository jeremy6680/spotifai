# db/models.py
# Pydantic models — define the shape of data exchanged between layers.
# These are NOT DuckDB table definitions (that's in database.py).
# Pydantic validates data at runtime and provides type safety.

# TODO: UserProfile model
# TODO: Playlist model
# TODO: PlaylistTrack model
# TODO: LLMCriteriaOutput model (the JSON structure returned by Claude)
