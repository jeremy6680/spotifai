# db/queries.py
# CRUD operations for DuckDB.
# All database reads and writes go through this module — never raw SQL in routes.

# TODO: upsert_user_profile()  — save or update user profile data
# TODO: get_user_profile()     — retrieve latest user profile
# TODO: save_playlist()        — persist a generated playlist to history
# TODO: get_playlist_history() — retrieve all past playlists for a user
# TODO: get_playlist_by_id()   — retrieve a single playlist by ID
